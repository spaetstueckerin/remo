<h1>Listing product_ranges</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_ranges.each do |product_range| %>
  <tr>
    <td><%= product_range.name %></td>
    <td><%= product_range.description %></td>
    <td><%= link_to 'Show', product_range %></td>
    <td><%= link_to 'Edit', edit_product_range_path(product_range) %></td>
    <td><%= link_to 'Destroy', product_range, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product range', new_product_range_path %>
