<h2>Gebäude</h2>

<p>An dieser Stelle sind alle Gebäude ihres Unternehmens gelistet. Bitte klicken Sie auf das Stiftsymbol, um das jeweilige Gebäude zubearbeiten. Mit Klick auf das grüne Pluszeichen können Sie ein neues Gebäude anlegen.</p>

<table class="zentriert">
  <tr>
    <th>Produktionsstandort</th>
    <th>Name</th>
    <th>Beschreibung</th>
    <th>Aktion</th>
  </tr>

<% @buildings.each do |building| %>
  <tr>
    <td><%= ProduktionSite.find_by_id(building.site_id).name %></td>
    <td><%= building.name %></td>
    <td><%= excerpt(building.description, " ", 9) %></td>

    <td><%= link_to image_tag("show.png", size: "20x20", alt: "Anzeigen"), building %>
    <%= link_to image_tag("edit.png", size: "20x20", alt: "Bearbeiten"), edit_building_path(building) %>
    <%= link_to image_tag("delete.png", size: "20x20", alt: "Löschen"), building, method: :delete, data: { confirm: 'Are you sure?' }  %></td>
  	</tr>
<% end %>
	<tr><th colspan = 3></th><th><%= link_to image_tag("new.png", size: "20x20", alt: "Anlegen"), new_building_path %></th></tr>
</table>