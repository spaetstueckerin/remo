<%= form_for(@contract) do |f| %>
  <% if @contract.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contract.errors.count, "error") %> prohibited this contract from being saved:</h2>

      <ul>
      <% @contract.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :energy_type, "Energieart" %><br />
    <%= f.select :energy_type, ["Strom", "Gas", "Wasser", "Öl", "Heizung"]%>
  </div>
  <div class="field">
    <%= f.label :supplier, "Energielieferant" %><br />
    <%= f.text_field :supplier %>
  </div>
  <div class="field">
    <%= f.label :description, "Beschreibung" %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :condition, "Konditionen" %><br />
    <%= f.text_field :condition %>
  </div>
  <div class="field">
    <%= f.label :duration_from, "Vertragsdauer von" %><br />
    <%= f.date_select :duration_from %>

    <%= f.label :duration_to, "bis" %> <%= f.date_select :duration_to %>
    <% user = User.find_by_id(session[:user_id]) %>
    <% enterprise = Enterprise.find_by_id(user.enterprise_id) %>
    <%= f.hidden_field :enterprise_id, :value => enterprise.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
