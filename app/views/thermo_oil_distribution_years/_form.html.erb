<%= form_for(@thermo_oil_distribution_year) do |f| %>
  <% if @thermo_oil_distribution_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thermo_oil_distribution_year.errors.count, "error") %> prohibited this thermo_oil_distribution_year from being saved:</h2>

      <ul>
      <% @thermo_oil_distribution_year.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
  <tr>
    <td><%= f.label :thermoOilConsumption_id, "Thermoölverbraucher" %></td>
    <td><%= f.number_field :thermoOilConsumption_id %></td>
    <td><%= f.label :thermoOilDistribution_id, "Thermoölverteilung" %></td>
    <td><%= f.number_field :thermoOilDistribution_id %></td>
  </tr> 
  <tr>
    <td><%= f.label :thermoOilProduction_id, "Thermoölproduktion" %></td>
    <td colspan="3"><%= f.number_field :thermoOilProduction_id %></td>
  </tr> 
  <tr>  
    <td><%= f.label :compression, "Druck" %></td>
    <td colspan="3"><%= f.number_field :compression %> bar Ü</td>
  </tr> 
  <tr>
    <td><%= f.label :flowTemperature, "Vorlauftemperatur" %></td>
    <td><%= f.number_field :flowTemperature %> °C</td>
    <td><%= f.label :returnTemperature, "Rücklauftemperatur" %></td>
    <td><%= f.number_field :returnTemperature %> °C</td>
  </tr>
</table>

  <div class="actions">
    <%= f.submit "Absenden" %>
  </div>
<% end %>
