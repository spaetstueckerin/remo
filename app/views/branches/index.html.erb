<h1>Branchen</h1>

<table class="zentriert">
  <tr>
    <th>Name</th>
    <th>Beschreibung</th>
    <th>Aktion</th>
  </tr>

<% @branches.each do |branch| %>
  <tr>
    <td><%= branch.name %></td>
    <td><%= branch.description %></td>
    
    <td><%= link_to image_tag("show.png", size: "20x20", alt: "Anzeigen"), branch %>
    <%= link_to image_tag("edit.png", size: "20x20", alt: "Bearbeiten"), edit_branch_path(branch) %>
    <%= link_to image_tag("delete.png", size: "20x20", alt: "Löschen"), branch, method: :delete, data: { confirm: 'Are you sure?' }  %></td>
    
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neue Branche', new_branch_path %>
