<%= form_for(@meter_type) do |f| %>
  <% if @meter_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meter_type.errors.count, "error") %> prohibited this meter_type from being saved:</h2>

      <ul>
      <% @meter_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
	<tr>
    <td><%= f.label :name, "Name" %></td>
    <td><%= f.text_field :name %></td>
</tr>
<tr>
    <td><%= f.label :description, "Beschreibung" %></td>
    <td><%= f.text_area :description %></td>
</tr>
</table>
  <div class="actions">
    <%= f.submit "Absenden" %>
  </div>
<% end %>
